// Write a C program to read an integer from the user and print it.

#include <stdio.h>

int main()
{
    int a,b;

    printf("Enter the value of a: ");
    scanf("%d",&a);
    printf("Enter the value of a: ");
    scanf("%d",&a);
    
    printf("The entered value of a is: %d",a);
    printf("The entered value of a is: %d",b);

    return 0;
}